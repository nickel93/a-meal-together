import { HeaderIcon } from "@/icon";
import { SurveyStart } from "@/widgets/survey";

const Page = () => {
  return (
    <div className="px-[20px] h-full  gap-[40px] flex flex-col">
      <HeaderIcon />
      <SurveyStart />
    </div>
  );
};

export default Page;
